"""docx / csv / xlsx の入出力。

``core`` に依存してよいが、``core`` からは参照しない(実装計画 §5)。

注意: このパッケージ名は標準ライブラリの ``io`` と同名だが、Python 3 の import は
既定で絶対 import なので、モジュール内の ``import io`` は標準ライブラリを指す。
名前は実装計画 §5 のディレクトリ構成に合わせている。
"""
