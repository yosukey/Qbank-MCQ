"""PySide6 の画面(実装計画 §5)。

画面 10 種は M4 で作る。いまここにあるのは実装計画 §2.2 のスパイク②
「PySide6 のウィンドウを出して SQLite を読むだけ」に相当する起動確認用の窓で、
配布物(exe / インストーラ)が動いていることと**バージョン**を目視できる。

``about.py`` は Qt に依存しない。表示文言はそこに置き、テストから直接叩ける
ようにしてある(``main_window.py`` は並べるだけ)。
"""
