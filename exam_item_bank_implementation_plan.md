# 口腔組織学 試験問題バンクシステム 実装計画

対象設計: 設計書 v16。用語は本書内では「誤答肢」(distractor)に統一する。

---

## 0. 開発方針

| 方針 | 理由 |
|---|---|
| **ロジック層を先に作り、GUIは後** | 正規化・導出・検証はすべてGUIなしでテストできる。先にここを固めると、GUI実装時にロジックのデバッグが混ざらない |
| **CLIを常に併走させる** | 各機能はまずCLIサブコマンドとして動かす。自動テストが書きやすく、GUI不調時の回避手段にもなる |
| **リスクの高い技術検証を最初に行う**(§2) | docx往復とPyInstallerは、後で発覚すると設計をやり直すことになる |
| **実データをテスト資産にする** | 2025年度の問題docxと集計CSVが揃っている。これをゴールデンファイルとして全期間使う |
| **スキーマ移行を初日から入れる** | exe配布後はユーザーがDBを直せない。移行機構を後付けするのは高くつく |

想定は単独開発(兼務)で、**週10〜15時間程度**。所要は§9参照。

## 1. 開発環境の準備(M0)

```
Qbank-MCQ/
├─ pyproject.toml          # 依存とツール設定を1ファイルに集約
├─ requirements.lock        # pip-compile 等で固定(exeビルドの再現性のため)
├─ src/qbank_mcq/
├─ tests/
├─ testdata/                # 実データのゴールデンファイル
└─ build/
```

- Python 3.11 系に固定(3.12以降はPyInstallerやQtの対応が遅れることがある)
- 仮想環境 + `requirements.lock` で依存を固定。**exeビルドの再現性はロックファイルに依存する**
- リポジトリはGitHub(private)。Issueで作業単位を管理
- リンタ/フォーマッタは ruff + black。型は pyright を緩めに
- ログは `logging` で `%APPDATA%\Qbank-MCQ\logs\` に出力(exe配布後、ユーザー環境の不具合はログしか手がかりがない)

**成果物**: 空のアプリが起動しログを吐く。`pytest` が0件で成功する。

## 2. スパイク(先行技術検証) ─ M1

設計の前提を壊しうる3点を、**本実装の前に単体スクリプトで潰す**。ここで想定と違えば設計を修正してから進む。

### 2.1 docx往復スパイク

2025年度の問題docxを対象に、

```
docx → run結合 → 書式判定 → HTML断片 → docx出力 → 再取込 → HTML断片
```

を通し、**1回目と2回目のHTMLが一致する**ことを確認する。

確認項目:
- `w:eastAsia` からMS明朝/MSゴシックが取れるか
- 上付き・下付きが往復するか
- 書式が同一の隣接runの結合が正しく効くか
- 均等割の全角空白が正しく除去・復元されるか
- 想定外の書式(下線、色、フォントサイズ等)がどれだけ混入しているか

**ここが最大のリスク**。想定外の書式が多ければ、許可タグを増やすか、無視して警告に留めるかを判断する。

### 2.2 PyInstallerスパイク

「PySide6のウィンドウを出してSQLiteを読むだけ」の最小アプリをexe化し、

- 未使用Qtモジュールをexcludeしたときのサイズ
- 起動時間(onedir)
- 別PC(開発環境なし)で動くか
- SmartScreenの警告がどう出るか

を確認する。**署名の要否判断をここで行う**。

### 2.3 データ層スパイク

集計CSVを読み、31パターン+空白から正答率・周辺マーク率・最頻誤答パターンを算出し、**シート記載の正答率・識別係数と突き合わせる**。設計書§9.2の検証チェーンが実データで全項目通ることを確認する。

**成果物**: 3本のスパイクスクリプトと結果メモ。設計変更が必要なら設計書を改訂してから次へ。

## 3. マイルストーン一覧

| # | 名称 | 内容 | 目安 | 完了時にできること |
|---|---|---|---|---|
| M0 | 準備 | 環境・リポジトリ・ログ | 0.5週 | — |
| M1 | スパイク | §2の3本 | 1週 | 技術的前提の確定 |
| M2 | データ層 | スキーマ・移行・ドメインロジック | 1.5週 | CLIでDB操作とロジック検証 |
| M3 | docx取込 | パーサ・セット同定・相互検証 | 1.5週 | **CLIで1回分をバンクに投入できる** |
| M4 | GUI基盤+バンク | 一覧・編集・改訂/派生 | 2週 | 画面で問題を作れる |
| M5 | 出題支援 | 選定・finalize・正答キー・冊子 | 2週 | **次回試験を作れる(最小実用単位)** |
| M6 | 統計取込・分析 | 検証チェーン・可視化・レポート | 2.5週 | サイクルが1周する |
| M7 | 配布 | exe・インストーラ・CI | 1週 | 配布可能 |
| M8 | 運用投入 | 過去問の一括投入と改善 | 随時 | 実運用 |

**M5完了が最初の実用到達点**。統計取込が未完成でも、バンクから選んで試験を作れる。

## 4. 各マイルストーンの詳細

### M2 データ層(1.5週)

**タスク**

1. SQLAlchemyモデル定義(設計書§8のスキーマ)
2. マイグレーション機構
   - `schema_meta.schema_version` を持ち、起動時に比較
   - 移行前に自動バックアップ(`backup/YYYYMMDD-HHMMSS.sqlite`)
   - 移行スクリプトは通し番号のPython関数として登録(Alembicは単独SQLiteには重い。自前で十分)
3. ドメインロジック(すべて純関数、GUI非依存)
   - `normalize_choice()` — NFKC + 均等割除去
   - `render_choice()` — 均等割復元
   - `derive_item_type(stem_html)` — タグ除去 → 指示文言パース → A/X2/X3/X4/XX
   - `validate_correct(correct, item_type)` — 個数整合
   - `strip_tags(html)` / `sanitize_html(html)` — 許可タグ4種のホワイトリスト
   - `choice_set_signature(items)` — ソート連結ハッシュ
   - `set_similarity(a, b)` — 共通項目数
   - `check_emphasis_rule(stem_html)` — 否定語と `<strong>` の整合
4. CLI骨格(`qbank db init` / `db migrate` / `db backup`)

**受入条件**

- `pytest` で上記関数のユニットテストが全通過
- 均等割テストに `Krause 小体` `滑膜 A 型細胞` `胎生 3-4 週` を含め、**変更されないこと**を明示的に検証
- タイプ導出テストに「すべて選べ・正答4個」(問23相当)を含める
- 旧版DBから新版DBへの移行が、バックアップを残して成功する

### M3 docx取込(1.5週)

**タスク**

1. docxパーサ
   - 段落走査、設問・選択肢の切り出し
   - 隣接run結合 → 書式判定 → HTML断片化
   - ノイズ除去(ページ番号、記名欄、`＜以上 50 設問＞` など)
   - インライン画像の抽出
2. 集計CSVパーサ(メタ行・31パターン+空白)
3. 突合と相互検証
   - 設問数の一致
   - docx由来のタイプ ⇔ CSV由来の正答個数
   - 強調規則チェック
4. 選択肢セットの同定と近似リンク生成
5. CLI(`qbank import-exam --docx X.docx --stats Y.csv --dry-run`)

**受入条件**

- 2025年度の実データで、**50問すべてが抽出され、相互検証の不整合が0件**
- 選択肢セットが期待どおり検出される(問1〜4、問5〜7、問8〜11 …)
- `--dry-run` で結果をJSONに出力し、目視確認できる
- 抽出結果をゴールデンファイルとして固定し、以後の回帰テストに使う

### M4 GUI基盤+バンク(2週)

**タスク**

1. メインウィンドウ、タブ構成、設定の読み書き
2. 問題バンク一覧(`QTableView` + `QSortFilterProxyModel`)
   - フィルタ: キーワード(タグ除去後)/タグ/タイプ/否定形/フラグ/draft/最終出題年
3. 問題編集ダイアログ
   - 設問本体(`QTextEdit`、書式ボタン4つ)
   - 指示文言ドロップダウン5種
   - 選択肢セット選択 + 並び順指定
   - 正答チェックボックス5個(タイプに応じた個数を即時検証)
   - **保存時に「改訂」/「派生」を選択**
4. 選択肢セット画面(一覧、近似リンク表示、「このセットで新しい問いを作る」)
5. 問題詳細画面(版履歴、派生系譜)

**受入条件**

- 新規作成・改訂・派生の3経路がそれぞれ正しいレコードを作る(統合テストで確認)
- タイプと正答個数の不整合が保存時にブロックされる
- draft保存と復帰ができる

### M5 出題支援(2週)

**タスク**

1. 選定エンジン(`select_candidates(conditions) -> list`)
   - タイプ別配分、タグ配分、識別係数下限、露出管理、新作混入率、セット/派生の重複制限、否定形上限
2. 試験セット編集画面(候補一覧 → 差し替え → 並べ替え)
3. finalize処理と変更ロック
4. 正答キーCSV出力
5. 問題冊子docx出力(テンプレート、2段組、書式復元、均等割復元)
6. 教員用照合表xlsx出力

**受入条件**

- 生成した冊子docxを**再取込したとき、元の問題と一致する**(往復テスト)
- finalize前チェックが全項目機能する(draft混入、派生同時出題、正答欠落など)
- 正答キーCSVがss-databaseに読み込める(実機確認)

### M6 統計取込・分析(2.5週)

**タスク**

1. 統計取込画面(試験選択 → CSV → 検証プレビュー → 確定)
2. 検証チェーン9項目の実装とエラー表示
3. 導出指標の算出と `item_stats` 書き込み、フラグ判定
4. 可視化(matplotlib埋め込み)
   - 周辺マーク率の横棒
   - **誤答パターン上位5件**
   - 部分正答分布
   - 正答率・識別係数の推移
5. 選択肢アイテム実績画面
6. 統計レポートxlsx出力(否定形層別を含む)
7. 取込後のフラグ一覧と改訂導線

**受入条件**

- 2025年度データで検証チェーン全項目が通り、算出値がシート記載値と一致
- わざと壊したCSV(列欠落、正答不一致、割合を人数と誤設定)で**確実にブロックされる**
- 問42・問19・問45が期待どおりのフラグを得る

### M7 配布(1週)

**タスク**

1. PyInstaller spec(onedir、Qtモジュールexclude、リソース同梱)
2. Inno Setupスクリプト(ユーザー単位インストール、データ保持アンインストール)
3. GitHub Actions(タグ push → exe+インストーラをRelease)
4. 別PCでのインストール・起動・DB移行の実機確認
5. バージョン通知(Releasesのversion.jsonを見るだけ)

**受入条件**

- 開発環境のないPCでインストールから起動、docx取込、統計取込まで通る
- 旧版インストール済みPCで上書きインストールしてもデータが保持され、移行が走る

## 5. モジュール構成

```
src/qbank_mcq/
├─ __main__.py            # CLIエントリ
├─ app.py                 # GUIエントリ
├─ core/
│  ├─ paths.py            # %APPDATA% 解決、初回セットアップ
│  ├─ db.py               # SQLAlchemyモデル・セッション
│  ├─ migrate.py          # スキーマ移行(通し番号方式)
│  ├─ text.py             # 正規化・均等割・タグ処理     ← 純関数
│  ├─ typing_rules.py     # タイプ導出・正答個数検証     ← 純関数
│  ├─ choiceset.py        # 署名・類似・リンク生成       ← 純関数
│  ├─ stats.py            # 度数からの導出・フラグ判定   ← 純関数
│  ├─ validate.py         # 検証チェーン
│  └─ selection.py        # 出題候補選定
├─ io/
│  ├─ docx_read.py        # 取込パーサ
│  ├─ docx_write.py       # 冊子出力
│  ├─ csv_stats.py        # 集計CSV
│  ├─ csv_key.py          # 正答キー
│  └─ xlsx_report.py      # レポート・照合表
├─ ui/
│  ├─ main_window.py
│  ├─ bank_view.py / question_editor.py / question_detail.py
│  ├─ choiceset_view.py / item_view.py
│  ├─ exam_builder.py / stats_import.py / settings.py
│  └─ widgets/            # リッチテキスト編集、正答チェック等
└─ resources/             # アイコン、冊子テンプレート
```

**`core/` の純関数群は `ui/` と `io/` に依存しない。** ここを守ると、ロジックのテストが常に高速で、GUI変更の影響を受けない。

## 6. テスト戦略

| 種別 | 対象 | 方法 |
|---|---|---|
| **ユニット** | `core/` の純関数 | pytest。正常系より**境界と反例**を厚く(均等割で削ってはいけない例、XX・正答4個、`<strong>` 誤用など) |
| **ゴールデンファイル** | docx取込結果 | 2025年度docxの抽出結果JSONを固定し、差分が出たらテスト失敗 |
| **往復テスト** | docx取込 ⇔ 冊子出力 | 取込→出力→再取込でHTMLが一致 |
| **異常系** | 検証チェーン | 壊したCSVを数種類用意し、期待どおりブロックされることを確認 |
| **統合** | 改訂/派生、finalize | インメモリSQLiteで一連の操作を実行し、レコード状態を検証 |
| **手動** | GUI | チェックリストを用意。自動化は費用対効果が低い |

`testdata/` に置くもの: 2025年度問題docx、集計CSV、抽出結果ゴールデンJSON、壊したCSV数種。

## 7. スキーマ移行の運用

```python
MIGRATIONS = {1: m001_initial, 2: m002_add_derived_from, ...}
```

- 起動時に `schema_version` を読み、不足分を順に適用
- **適用前に必ずバックアップ**。失敗したらバックアップを戻して起動を中止し、ログに理由を残す
- 移行テストは「旧バージョンのDBファイルを `testdata/` に保存し、移行が通ることを確認」する。**バージョンを上げるたびに旧DBファイルを1つ残す**

## 8. ビルドとリリース

```
git tag v0.3.0 && git push --tags
  → GitHub Actions (windows-latest)
      → pip install -r requirements.lock
      → pytest
      → PyInstaller (onedir)
      → Inno Setup
      → Release に Qbank-MCQ-0.3.0-setup.exe を添付
```

- バージョンは `0.x` の間は互換性を保証しない。実運用開始時に `1.0` とする
- リリースノートに**スキーマ変更の有無**を必ず明記

## 9. 見積もりとスケジュール

| 期間 | マイルストーン |
|---|---|
| 第1〜2週 | M0 + M1(準備とスパイク) |
| 第3〜4週 | M2(データ層) |
| 第5〜6週 | M3(docx取込) |
| 第7〜8週 | M4(GUI基盤+バンク) |
| 第9〜10週 | M5(出題支援)← **ここで実用可能** |
| 第11〜13週 | M6(統計取込・分析) |
| 第14週 | M7(配布) |

週10〜15時間で**約14週**。まとまった時間が取れれば短縮できるが、**M1のスパイクとM7の配布は圧縮しにくい**(外部要因に左右されるため)。

**次回試験の作成に間に合わせたい場合は、M5(第10週)を締切として逆算する。** 統計取込(M6)は試験実施後で構わない。

## 10. 進行管理

- マイルストーンごとにGitHubのMilestoneを切り、タスクをIssue化
- 各マイルストーンの**受入条件を最初にテストとして書く**(受入条件が曖昧なまま進めない)
- 週末に15分、「受入条件のうち何が通ったか」だけ記録する。詳細な進捗管理は単独開発では負担が上回る

## 11. 先に知っておくべき実装上の落とし穴

| 箇所 | 内容 |
|---|---|
| docx | `run.font.name` では日本語フォントが取れない。`w:rFonts/@w:eastAsia` を直接読む |
| docx | 1つの語が複数runに分割される。書式が同一の隣接runを先に結合する |
| 正規化 | 空白の一律削除は `Krause 小体` を壊す。均等割規則は「日本語1文字+空白+日本語1文字」に限定 |
| 正規化 | NFKCはU+3000を半角空白に変える。均等割の正規表現が両方を許容していれば順序に依存しない |
| 検索 | 否定形設問はタグが語中に入る。検索・指示文言パースは必ずタグ除去後に行う |
| CSV | 正答率が丸め値だと精度を失う。**正答数/N から再計算して保存** |
| CSV | 「人数か割合か」の取り違えは静かに全統計を壊す。非整数検出で警告 |
| exe | Program Files配下は書込不可。データは `%APPDATA%` へ |
| exe | onefileは起動が遅く誤検知も多い。onedir + インストーラ |
| Qt | 未使用モジュールをexcludeしないとexeが数百MB膨らむ |
| matplotlib | 日本語フォントを rcParams で明示しないと豆腐になる |

## 12. 最初の2週間でやること(具体)

1. リポジトリ作成、Python 3.11の仮想環境、`pytest` が動くところまで
2. **2025年度の問題docxと集計CSVを `testdata/` に置く**
3. スパイク①: docxを読んで全runの `(text, eastAsia, ascii, bold, italic, sup, sub)` をダンプ。**想定外の書式がどれだけあるかを目で確認する**
4. スパイク②: 最小PySide6アプリをexe化し、別PCで起動
5. スパイク③: 集計CSVから正答率・識別係数を再計算し、シート記載値と一致するか確認

この5つが済めば、設計の技術的前提はすべて裏が取れる。以後は迷いなく実装に入れる。
