# 実行時依存の固定(実装計画 §1)。
#
#   **exe ビルドの再現性はロックファイルに依存する。**
#   GitHub Actions は `pip install -r requirements.lock` を使う(実装計画 §8)。
#
# 更新するとき:
#   python -m venv .venv && .venv/bin/pip install -e ".[dev,gui]"
#   .venv/bin/pip freeze でバージョンを確認し、ここを書き換えて pytest を通す。
#
# 配布(PyInstaller)は M7 で追加する。

SQLAlchemy==2.0.52
et_xmlfile==2.0.0
greenlet==3.5.5
lxml==6.1.2
openpyxl==3.1.5
python-docx==1.2.0
typing_extensions==4.16.0

# GUI(M4)。PySide6 は LGPL で再配布制約が緩い(設計書 §15)。
# Linux の CI で走らせるには libEGL などの共有ライブラリが要る
# (.github/workflows/test.yml を参照)。
PySide6==6.11.2
PySide6_Addons==6.11.2
PySide6_Essentials==6.11.2
shiboken6==6.11.2

# 可視化(M6 の設計書 §14-3)。scipy は不要(設計書 §15)。
matplotlib==3.11.1
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
numpy==2.4.6
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
