# 実行時依存の固定(実装計画 §1)。
#
#   **exe ビルドの再現性はロックファイルに依存する。**
#   GitHub Actions は `pip install -r requirements.lock` を使う(実装計画 §8)。
#
# 更新するとき:
#   python -m venv .venv && .venv/bin/pip install -e ".[dev]"
#   .venv/bin/pip freeze でバージョンを確認し、ここを書き換えて pytest を通す。
#
# GUI と配布(PySide6 / matplotlib / PyInstaller)は M4・M7 で追加する。

SQLAlchemy==2.0.52
et_xmlfile==2.0.0
greenlet==3.5.5
lxml==6.1.2
openpyxl==3.1.5
python-docx==1.2.0
typing_extensions==4.16.0
