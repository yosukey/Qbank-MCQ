# 画面(M4)と可視化(M6)を動かすときの依存。
#
#   pip install -r requirements-gui.lock
#
# CI のテストはこれを使う。**GUI テストを skip させないため**で、PySide6 が入って
# いないと `tests/test_ui_*.py` が丸ごと飛び、10 画面が壊れても気づけない。
# Linux では libEGL などの共有ライブラリも要る(`.github/workflows/test.yml`)。

-r requirements.lock

# GUI(M4)。PySide6 は LGPL で再配布制約が緩い(設計書 §15)。3 つは版を揃える。
PySide6==6.11.2
PySide6_Essentials==6.11.2
PySide6_Addons==6.11.2
shiboken6==6.11.2

# 可視化(設計書 §14-3 の周辺マーク率・誤答パターン・推移)。**scipy は不要**(§15)。
matplotlib==3.11.1
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
numpy==2.4.6
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
